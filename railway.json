{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "phases": {
      "build": {
        "cmds": ["mkdir -p storage/framework/{sessions,views,cache,testing} storage/logs bootstrap/cache && chmod -R a+rw storage"]
      }
    }
  },
  "deploy": {
    "startCommand": "php artisan config:cache && php artisan route:cache && php -S 0.0.0.0:$PORT -t public"
  }
}